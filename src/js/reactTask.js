import React from 'react'

const ReactTask = () => {
    const data = ['Trupti','Mayur','Sargram','Pooja','Tejas']

  return (
    <div>
      <button onClick={handleClick}>view data</button>
    </div>
  )
}

export default ReactTask
